export enum TagTypes {
  admin = "admin",
  user = "user",
  provider = "provider",
  superadmin = "superadmin",
}

export const tagTypes = [
  TagTypes.admin,
  TagTypes.user,
  TagTypes.provider,
  TagTypes.superadmin,
];
